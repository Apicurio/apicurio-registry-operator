apiVersion: registry.apicur.io/v1
kind: ApicurioRegistry
metadata:
  name: example-apicurioregistry-kafkasql
spec:
  configuration:
    persistence: "kafkasql"
    kafkasql:
      bootstrapServers: "<service name>.<namespace>.svc:9092"
      # Try using Strimzi/Red Hat AMQ Streams Operator!
      # Optional security configuration, using TLS or SCRAM:
      # security:
      #   tls:
      #     truststoreSecretName: "..."
      #     keystoreSecretName: "..."
      #   scram:
      #     truststoreSecretName: "..."
      #     user: "..."
      #     passwordSecretName: "..."
      #     mechanism: "SCRAM-SHA-512" # Optional (default value)
      # The secrets above are generated by Strimzi/Red Hat AMQ Streams,
      # see documentation if you want to create them manually.
