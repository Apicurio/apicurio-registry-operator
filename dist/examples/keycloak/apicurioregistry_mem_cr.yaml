apiVersion: registry.apicur.io/v1
kind: ApicurioRegistry
metadata:
  name: example-apicurioregistry
spec:
  configuration:
    security:
      keycloak:
        url: ""
        # ^ Required: Keycloak server URL, must end with /auth
        realm: "registry"
        # apiClientId: "registry-client-api"
        # ^ Default value
        # uiClientId: "registry-client-ui"
        # ^ Default value

